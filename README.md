Clone Project, Flask im Pycharm installieren, fuer authenticationsql.py -> bcrypt package installieren, fue flask_session -> Terminal im Pycharm oeffnen-> pip (oder pip3) und command “pip install Flask-Session”
